## Session: Medical IFU Search MVP (OpenIFU) - 2026-03-14

### Actions Taken
- Built out the end-to-end data acquisition and RAG pipeline for medical device Instructions for Use (IFU).
- **Database:** Supabase with `pgvector` enabled (768 dimensions for `models/gemini-embedding-001`).
- **Backend:** Ripped out `llama-index` due to heavy dependencies/C-compiler issues on macOS. Rewrote `backend/app/ingest.py` and `backend/app/main.py` using native `PyMuPDF` (fitz), raw `google-generativeai`, and Supabase python client.
- **Frontend:** Built a lightweight, OpenEvidence-style UI using Streamlit (`frontend/app.py`). Tested successfully with a real Stryker battery drill IFU (`7200-001-700_Rev AD.pdf`).
- **Scraper Strategy:** Pivot from generic web scraping to targeted strikes on manufacturer internal APIs. Built initial script (`backend/scraper/stryker_strike.py`) and FDA recon script (`backend/scraper/fda_recon.py`).

### Current State / Blockers
- Working with Sean to extract the exact internal API request (via Chrome DevTools Network tab -> Copy as cURL) from the Stryker eIFU portal (`https://labeling.stryker.com/hcp`).
- Specifically targeting the `products?audience=HCP...` request to reverse-engineer their database query structure and authentication headers.

### Operator Notes
- Sean struggles slightly with developer tools but follows exact, step-by-step instructions well when given visual cues. Keep instructions strictly functional.
- Security protocols enforced: Caught and burned two pasted Supabase secret keys in chat; moved keys safely to `backend/.env`.
- Emphasize speed and leverage: Streamlit + direct API calls beat heavy frameworks for MVP validation with doctors.

### Transition to SWT Project
- Sean signed off for the night.
- Shifted focus to Snow White Tech (SWT) - a snowboard binding/accessory company (www.snowwhitetech.com).
- Product: "The Speed Strap" (aftermarket binding retrofit for quick-entry).
- Role: Operating as Creative Director / VC overnight to audit the site and build a growth/optimization strategy.
- Created `swt/strategy.md` with initial teardown and VC thesis.
