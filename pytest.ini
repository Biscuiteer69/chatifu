[pytest]
testpaths = tests
norecursedirs = backup_* .venv venv qdrant fda run __pycache__
addopts = -m "not integration"
markers =
    integration: hits the live vault API + manufacturer IFUs (network); run before deploy
